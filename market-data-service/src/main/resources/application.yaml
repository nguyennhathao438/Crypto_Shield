spring:
  application:
    name: market-data-service
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: priceEvent:com.crypto_shield.market_data_service.dto.PriceResponse
    consumer:
      group-id: market-data-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "com.crypto_shield.*"
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.crypto_shield.market_data_service.dto.SymbolDemandEvent
server:
  port: 8084
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}
binance:
  rest-base-url: https://fapi.binance.com
  ws-base-url: wss://fstream.binancefuture.com/ws
  kline-path: /fapi/v1/klines

