spring:
  application:
    name: cryptoshield
  datasource:
    url: jdbc:postgresql://localhost:5433/crypto_db
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true